# I don't know screen well enough to be sure if these are needed

# ## Control-t  is `transpose' in Emacs, which I never use
# escape ^T^T

# ## do not trash BackSpace, usually DEL
# bindkey -k kb
# bindkey -d -k kb

# ## do not trash Delete, usually ESC [ 3 ~
# bindkey -k kD
# bindkey -d -k kD

term screen-256color
