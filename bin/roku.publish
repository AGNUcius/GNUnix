#!/bin/bash

if [[ "$3" == "" ]]; then
   echo "Arg1: Roku IP.Add.re.ss or MA:CA:DD:RE:SS"
   echo "Arg2: Username:Password"
   echo "Arg3: Directory"
   exit 22
fi

roku.pkg "$1" "$3.zip"
roku.add "$@"
