;; menu-clock.el --- show the time as a menu item

;; (define-key menu-bar-clock [next-tag]
;;   '(menu-item "show time/date"
;; 			  current-time-string
;; 			  :enable t
;; 			  :help "tooltip"))

;; (defvar menu-bar-clock (make-sparse-keymap "clock"))
;; (define-key global-map [menu-bar clock] (cons (format-time-string "%l:%M %p  %a %b %e, %y") menu-bar-clock))
;; (define-key global-map [menu-bar clock] nil)